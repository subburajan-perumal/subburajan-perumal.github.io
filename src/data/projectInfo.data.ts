export interface ProjectInfoProps {
  id: number;
  projectName: string;
  client: string;
}

const projectInfo = [
  {
    id: 1,
    projectName: 'Algolia Search Integration',
    client: 'Testlify',
  },
];

export default projectInfo;
